# Running Biomechanics MCP — Проект анализа биомеханики бега

## Обзор проекта

Этот проект представляет собой систему для анализа биомеханики бега с использованием искусственного интеллекта GigaChat. Система получает данные о движениях бегуна (углы суставов, положение корпуса и т.д.), анализирует их, выявляет ошибки в технике и даёт персонализированные рекомендации с упражнениями для улучшения.

**Основные возможности:**
- Детальный анализ техники бега с оценкой по 6 категориям
- Автоматическое выявление ошибок и проблем в технике
- Персонализированные рекомендации с упражнениями
- Определение зон фокуса для тренировок
- HTTP API для интеграции с мобильными приложениями и веб-сервисами
- Поддержка MCP (Model Context Protocol) для работы с AI-ассистентами

## Архитектура — монорепозиторий на Turborepo

Проект организован как монорепозиторий с несколькими взаимосвязанными пакетами:

```
running-biomechanics-mcp/
├── packages/               # Библиотеки
│   ├── types/              # @biomech/types
│   ├── calculators/        # @biomech/calculators
│   └── gigachat-client/    # @biomech/gigachat-client
└── apps/                   # Приложения
    └── mcp-server/         # @biomech/mcp-server
```

### Описание пакетов

#### `@biomech/types`
**Назначение:** Общие TypeScript типы и Zod-схемы для валидации данных.

**Что содержит:**
- `RunBiomechanicsInput` — входные данные о биомеханике бега (углы суставов, положение корпуса)
- `RunAnalysisResult` — результат детального анализа с оценками и метриками
- `ErrorDetectionResult` — результат выявления ошибок
- `RecommendationResult` — персонализированные рекомендации
- `JsonAnalysisResult` — структурированный JSON-ответ для фронтенда
- Zod-схемы для валидации всех входных данных

#### `@biomech/calculators`
**Назначение:** Библиотека математических вычислений и анализа биомеханики.

**Что содержит:**
- **Калькуляторы оценок:** расчёт качества работы рук, ног, стабильности корпуса, симметрии, эффективности и консистентности движений
- **Функции анализа:** `analyzeRunBiomechanics()` — комплексный анализ техники бега с итоговой оценкой
- **Выявление ошибок:** `detectErrors()` — автоматическое определение проблем в технике на основе пороговых значений
- **Рекомендации:** `getRecommendations()` — подбор упражнений для устранения выявленных ошибок
- **Определение фокуса:** `getFocusAreas()` — выбор приоритетных зон для тренировок
- **Нормы и данные:** эталонные значения биомеханических показателей, база упражнений
- **Утилиты:** математические функции (нормализация, расчёт асимметрии, CV и т.д.)

**Не зависит от GigaChat** — все вычисления выполняются локально на основе математических моделей.

#### `@biomech/gigachat-client`
**Назначение:** Клиент для работы с GigaChat API.

**Что содержит:**
- `GigaChatService` — базовый сервис для отправки запросов в GigaChat
- `GigaChatMCPFull` — оркестратор для MCP-сценариев (последовательный диалог с function calling)
- `JsonAnalysisAgent` — агент для генерации JSON-ответов с описательными текстами на естественном языке

**Зависимости:** `@biomech/types`, `@biomech/calculators`, библиотека `gigachat`

#### `@biomech/mcp-server`
**Назначение:** HTTP-сервер с REST API для анализа биомеханики.

**Что содержит:**
- **HTTP API:**
  - `POST /analyze` — анализ биомеханики с генерацией JSON-ответа через GigaChat
  - `GET /health` — проверка статуса сервера
- **MCP Tools:** набор инструментов для работы по протоколу Model Context Protocol (опционально, закомментирован)
- **Примеры использования** в папке `examples/`

**Зависимости:** все пакеты `@biomech/*`, Express, MCP SDK

## Установка

### Требования
- **Bun** >= 1.1.0 (установить: `curl -fsSL https://bun.sh/install | bash`)
- **Node.js** >= 20 (опционально, для совместимости)

### Установка зависимостей

```bash
cd running-biomechanics-mcp
bun install
```

Bun установит все зависимости для всех пакетов в монорепозитории.

## Сборка

Собрать все пакеты:

```bash
bun run build
```

Turborepo автоматически соберёт пакеты в правильном порядке:
1. `@biomech/types`
2. `@biomech/calculators`
3. `@biomech/gigachat-client`
4. `@biomech/mcp-server`

Результаты сборки будут в папках `dist/` каждого пакета.

Очистить результаты сборки:

```bash
bun run clean
```

## Запуск

### Конфигурация

Создайте файл `.env` в корне проекта или в `apps/mcp-server/`:

```env
GIGACHAT_API_KEY=ваш_ключ_api
GIGACHAT_API_URL=https://gigachat.devices.sberbank.ru/api/v1
GIGACHAT_MODEL=GigaChat
PORT=3000
```

**Как получить API ключ:**
1. Зарегистрируйтесь на [developers.sber.ru](https://developers.sber.ru)
2. Создайте проект и получите credentials для GigaChat API
3. Скопируйте Authorization данные в переменную `GIGACHAT_API_KEY`

### Запуск сервера

```bash
cd apps/mcp-server
bun run dev
```

Или из корня проекта:

```bash
bun run dev
```

Сервер запустится на `http://localhost:3000` (или на порту из `PORT`).

Вы увидите в консоли:
```
🚀 HTTP Server running on port 3000
📊 POST /analyze endpoint ready
💚 GET /health endpoint ready
```

### Запуск для продакшена

```bash
cd apps/mcp-server
bun run start
```

## Использование

### Тестирование с curl

Проверка здоровья сервера:

```bash
curl http://localhost:3000/health
```

Ответ:
```json
{
  "status": "ok",
  "service": "running-biomechanics-mcp"
}
```

Анализ биомеханики бега:

```bash
curl -X POST http://localhost:3000/analyze \
  -H "Content-Type: application/json" \
  -d @apps/mcp-server/tests/fixtures/real-data.json
```

**Формат входных данных** (см. `real-data.json`):
```json
{
  "left_arm": {
    "arm_swing": { "min": 142.5, "max": 160.6, "mean": 151.6, "std": 5.2, "count": 78 },
    "elbow_angle": { "min": 91.3, "max": 139.3, "mean": 115.3, "std": 10.8, "count": 82 }
  },
  "right_arm": { ... },
  "left_leg": {
    "knee_angle": { ... },
    "ankle_angle": { ... },
    "hip_angle": { ... },
    "shank_angle": { ... }
  },
  "right_leg": { ... },
  "trunk": {
    "trunk_angle": { ... }
  },
  "head": {
    "head_angle": { ... }
  }
}
```

**Формат ответа:**
```json
{
  "first-message": "Общий вывод о технике бега...",
  "blocks": [
    {
      "id": "technique",
      "message": "Детальный анализ техники...",
      "data": {
        "score": 87.5,
        "level": "Продвинутый",
        "metrics": [
          { "name": "Руки", "value": 92 },
          { "name": "Ноги", "value": 85 },
          { "name": "Корпус", "value": 88 },
          { "name": "Стабильность", "value": 86 }
        ]
      }
    },
    {
      "id": "focus",
      "message": "Зона фокуса для тренировок...",
      "recommendation": "Конкретные советы...",
      "estimated_improvement": "2-3 недели"
    },
    {
      "id": "errors",
      "message": "Выявленные ошибки...",
      "data": [
        {
          "priority": "high",
          "description": "Асимметрия в работе рук..."
        }
      ]
    },
    {
      "id": "exercises",
      "message": "Рекомендации по упражнениям...",
      "data": [
        {
          "id": "high_knees",
          "name": "Бег с высоким подниманием бедра",
          "description": "Техника выполнения: ..."
        }
      ]
    }
  ]
}
```

### Интеграция в приложение

**JavaScript/TypeScript:**
```typescript
async function analyzeBiomechanics(data: RunBiomechanicsInput) {
  const response = await fetch('http://localhost:3000/analyze', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(data)
  });
  
  if (!response.ok) {
    throw new Error('Analysis failed');
  }
  
  const result = await response.json();
  return result;
}
```

**Python:**
```python
import requests

def analyze_biomechanics(data):
    response = requests.post(
        'http://localhost:3000/analyze',
        json=data
    )
    response.raise_for_status()
    return response.json()
```

## Переменные окружения

| Переменная | Описание | Обязательна | По умолчанию |
|------------|----------|-------------|--------------|
| `GIGACHAT_API_KEY` | API ключ для GigaChat | ✅ Да | - |
| `GIGACHAT_API_URL` | URL GigaChat API | ❌ Нет | `https://gigachat.devices.sberbank.ru/api/v1` |
| `GIGACHAT_MODEL` | Модель GigaChat | ❌ Нет | `GigaChat` |
| `PORT` | Порт HTTP сервера | ❌ Нет | `3000` |

## Структура проекта (детально)

```
running-biomechanics-mcp/
├── docs/                           # Документация
│   ├── project.md                  # Этот файл
│   └── turborepo.md                # Про Turborepo
├── packages/
│   ├── types/
│   │   ├── src/
│   │   │   ├── biomechanics.ts     # Типы биомеханических данных
│   │   │   ├── json-result.ts      # Типы JSON-ответов
│   │   │   ├── schemas.ts          # Zod-схемы для валидации
│   │   │   └── index.ts
│   │   ├── package.json
│   │   └── tsconfig.json
│   ├── calculators/
│   │   ├── src/
│   │   │   ├── calculators/        # Калькуляторы метрик
│   │   │   ├── engines/            # Движки анализа
│   │   │   ├── data/               # Нормы и упражнения
│   │   │   ├── utils/              # Математические утилиты
│   │   │   ├── analyze-run.ts      # Комплексный анализ
│   │   │   ├── detect-errors.ts    # Выявление ошибок
│   │   │   ├── get-recommendations.ts
│   │   │   └── index.ts
│   │   ├── package.json
│   │   └── tsconfig.json
│   └── gigachat-client/
│       ├── src/
│       │   └── gigachat/
│       │       ├── gigachat.service.ts
│       │       ├── gigachat-orchestrator.ts
│       │       └── types.ts
│       ├── package.json
│       └── tsconfig.json
└── apps/
    └── mcp-server/
        ├── src/
        │   ├── index.ts            # HTTP сервер
        │   ├── tools/              # MCP инструменты
        │   └── gigachat/
        │       └── json-agent.ts   # JSON агент
        ├── examples/               # Примеры использования
        │   ├── direct-analysis.ts
        │   ├── json-analysis.ts
        │   └── sequential-analysis.ts
        ├── tests/
        │   └── fixtures/
        │       ├── real-data.ts
        │       └── real-data.json  # Для curl
        ├── package.json
        └── tsconfig.json
```

## Разработка

### Добавление нового функционала

1. **Новый калькулятор метрики** → добавить в `packages/calculators/src/calculators/`
2. **Новый тип данных** → добавить в `packages/types/src/`
3. **Новый HTTP endpoint** → добавить в `apps/mcp-server/src/index.ts`

### Запуск в режиме разработки

```bash
bun run dev
```

TypeScript будет пересобираться автоматически при изменениях файлов благодаря Bun.

### Тестирование изменений

После изменения кода в `packages/*`:
1. Пересоберите пакеты: `bun run build`
2. Перезапустите сервер: `bun run dev`
3. Протестируйте через curl

## Производительность

- **Время анализа:** 3-10 секунд (зависит от скорости GigaChat API)
- **Математические вычисления:** < 100 мс (локально, без AI)
- **Поддержка конкурентных запросов:** да (Express + async/await)

## Roadmap

- [ ] Добавить кеширование результатов анализа
- [ ] Websocket API для стриминга результатов
- [ ] Поддержка batch-анализа (массовая обработка)
- [ ] Визуализация данных (графики, диаграммы)
- [ ] Хранение истории анализов пользователя
- [ ] Сравнение результатов во времени (прогресс)

## Поддержка

По вопросам и проблемам создавайте issues в репозитории проекта.
